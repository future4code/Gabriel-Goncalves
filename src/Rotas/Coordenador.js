export const exibeLogin = (history) => {
    history.push('/')
}

export const exibeFeed = (history) => {
    history.push('/feed')
}

export const exibeCadastro = (history) => {
    history.push('/cadastro')
}

export const exibePost = (history) => {
    history.push('/post')
}